var tarjeta = "";

	do{
		var pregunta = prompt("Indique número de su tarjeta");
		var reverse = pregunta.reverse(pregunta);
			
		
	} while (pregunta == "reverse" || (pregunta != "" && pregunta != ""));



function inverso(tarjeta){
	for (var i = 0; i < tarjeta.reverse; i++) {
		Things[i]
	}
	alert("Hola");
}
